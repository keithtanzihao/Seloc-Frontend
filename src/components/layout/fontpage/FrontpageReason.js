import React from "react";

export default class FrontpageReason extends React.Component {
  render() {
    return (
      <div className={`${styles["frontpg__reason"]}`}>
        <div className={`${styles["frontpg__reasonCtn"]}`}>
          <picture>
            
          </picture>
        </div>
      </div>
    );
  }
}
